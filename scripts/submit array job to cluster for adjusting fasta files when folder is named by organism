module load orthomcl

qsub -I $PBS_ARRAYID orthomclAdjustFasta $folder $fastafile 1 


# folders must be a three or four letter unique abbreviation for the taxon (can grep out the genus name beforehand)

for folder in */
do
 echo $folder
 for fastafile in "$folder"*.fasta*
  do
    echo $fastafile
  done
done
